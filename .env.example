# SEPStack development / integration test environment variables
# Copy to .env and fill in values — never commit .env

# Anchor domain for integration tests (e.g. testanchor.stellar.org)
TEST_ANCHOR_DOMAIN=

# Stellar secret key for SEP-10 integration tests (S...)
TEST_STELLAR_SECRET=

# Network to use for integration tests
TEST_NETWORK=testnet
